# API

The MVP has no backend API. This is intentional.

Financial data, tax calculations, bank statements, salary slips, prompts, CNIC, asset details, and liability details must not be sent to a backend.

A future optional metadata service may serve public rule manifests, version checks, and health checks only.
